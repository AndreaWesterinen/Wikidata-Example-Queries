# rewrite_status: rewritten
#title: Types of relationships that link painters to anonymous paintings
#defaultView:BubbleChart
PREFIX  p:    <http://www.wikidata.org/prop/>
PREFIX  rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX  rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX  wd:   <http://www.wikidata.org/entity/>
PREFIX  wdt:  <http://www.wikidata.org/prop/direct/>
PREFIX  wikibase: <http://wikiba.se/ontology#>

SELECT  ?qualifierpropertyLabel (COUNT(?creatorqualifier) AS ?count)
WHERE
  { ?item  wdt:P31   wd:Q3305213 ;
           wdt:P170  ?creatorvalue
    FILTER contains(str(?creatorvalue), "wikidata.org/.well-known/genid")
    OPTIONAL
      { ?item     p:P170              ?creatorstatement .
        ?creatorstatement
                  rdf:type            wikibase:BestRank ;
                  ?qualifier          ?creatorqualifier .
        ?qualifierproperty
                  wikibase:qualifier  ?qualifier
      }
    FILTER ( ?qualifierproperty != wd:P3831 )
    BIND(if(bound(?qualifierproperty), ?qualifierproperty, "unbound") AS ?qualifierproperty__label_subject)
    OPTIONAL
      { ?qualifierproperty__label_subject
                  rdfs:label  ?qualifierproperty__label_value
        FILTER ( lang(?qualifierproperty__label_value) = "en" )
      }
    OPTIONAL
      { ?qualifierproperty__label_subject
                  rdfs:label  ?qualifierproperty__label_value
        FILTER ( lang(?qualifierproperty__label_value) = "mul" )
      }
    BIND(if(bound(?qualifierproperty__label_value), ?qualifierproperty__label_value, if(( isURI(?qualifierproperty) && strstarts(str(?qualifierproperty), "http://www.wikidata.org/entity/") ), strafter(str(?qualifierproperty), "entity/"), str(?qualifierproperty))) AS ?qualifierpropertyLabel)
  }
GROUP BY ?qualifierpropertyLabel
HAVING ( ?count > 2 )
LIMIT   2000

# Original query:
# #title: Types of relationships that link painters to anonymous paintings
# #defaultView:BubbleChart
# SELECT ?qualifierpropertyLabel (COUNT(?creatorqualifier) AS ?count) WHERE {
#   ?item wdt:P31 wd:Q3305213 .
#   ?item wdt:P170 ?creatorvalue .
#   FILTER wikibase:isSomeValue(?creatorvalue)
#   OPTIONAL { ?item p:P170 ?creatorstatement .
#              ?creatorstatement rdf:type wikibase:BestRank .
#              ?creatorstatement ?qualifier ?creatorqualifier .
#              ?qualifierproperty wikibase:qualifier ?qualifier }
#   FILTER(?qualifierproperty != wd:P3831)
#   SERVICE wikibase:label { bd:serviceParam wikibase:language "en" }
#   } GROUP BY ?qualifierpropertyLabel
# HAVING (?count > 2)
# LIMIT 2000
