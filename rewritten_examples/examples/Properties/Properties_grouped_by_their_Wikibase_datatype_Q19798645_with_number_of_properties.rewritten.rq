# rewrite_status: rewritten
#title: Properties grouped by their type with number of properties
PREFIX  rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX  wikibase: <http://wikiba.se/ontology#>

SELECT  (COUNT(?property) AS ?pcount) ?wbtype
WHERE
  { ?property  rdf:type             wikibase:Property ;
              wikibase:propertyType  ?wbtype
  }
GROUP BY ?wbtype
ORDER BY DESC(?pcount)

# Original query:
# #title: Properties grouped by their type with number of properties
# SELECT (COUNT(?property) as ?pcount ) ?wbtype WHERE {
#   ?property rdf:type wikibase:Property.
#   ?property wikibase:propertyType ?wbtype.
#   SERVICE wikibase:label { bd:serviceParam wikibase:language "[AUTO_LANGUAGE],mul,en". }
# }
# GROUP BY ?wbtype
# ORDER BY DESC(?pcount)
